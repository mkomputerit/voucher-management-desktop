# Direct runtime dependencies. Versions must match requirements-lock.txt,
# which is the hash-verified environment used by Windows CI/release builds.
reportlab==4.5.1
Pillow==12.3.0
pypdfium2==4.30.0
pywin32==311
sv-ttk==2.6.1
darkdetect==0.8.0
cryptography==50.0.1
