# Exact versions captured from a successful Windows GitHub Actions build.
# Update deliberately and rerun the full Windows build/test pipeline.
reportlab==4.5.1
Pillow==12.3.0
pypdfium2==4.30.0
pywin32==311
sv-ttk==2.6.1
darkdetect==0.8.0
